#!/bin/bash
set -euo pipefail


# WS method not supported for bash
# WS method not supported for bash
# WS method not supported for bash
# WS method not supported for bash
# WS method not supported for bash
# None method not supported for bash
# WS method not supported for bash
# WS method not supported for bash
# WS method not supported for bash
# None method not supported for bash
