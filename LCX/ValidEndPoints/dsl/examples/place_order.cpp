#include <iostream>
#include <curl/curl.h>
#include <nlohmann/json.hpp>

# POST method not supported for cpp
std::string check_order_status(order_id) {
    std::string url = "https://exchange-api.lcx.com/api/trading/order/{order_id}";
    // GET request implementation
    auto response = http_client.get(url);
    return response.body();
}
# POST method not supported for cpp
std::string complete_order_flow(currency) {
    std::string url = "https://exchange-api.lcx.com/api/account/balance";
    // GET request implementation
    auto response = http_client.get(url);
    return response.body();
}


# POST method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
# None method not supported for cpp
